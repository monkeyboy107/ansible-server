- name: Setup and configure ceph pools
  hosts: ceph_mgrs
  become: yes
  gather_facts: yes
  tasks:
    - name: Setup and configure ceph pools
      include_role:
        - ceph-add-pool
      loop: "{{ pools_path_list }}"
      include_vars: "{{ item }}"
