- name: Setup and configure ceph pools
  hosts: ceph_mgrs
  become: yes
  gather_facts: yes
  tasks:
   - name: Setup and configure ceph pools
     import_role:
       name: ceph-add-pool
     loop: "{{ pools_path_list }}"
     include_vars: "{{ item }}"
